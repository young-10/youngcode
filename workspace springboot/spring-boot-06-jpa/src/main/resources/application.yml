server:
  port: 8181
spring:
  datasource:
    username: root
    password: qy562704
    url: jdbc:mysql://localhost:3306/test
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      #更新或创建数据表结构
      ddl-auto: update
      #控制台显示SQL
    show-sql: true